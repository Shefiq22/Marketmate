class AppConfig {
  AppConfig._();

  static const String baseUrl = 'https://marketmate-kmn1.onrender.com';
  static const Duration requestTimeout = Duration(seconds: 60);
  static const Duration uploadTimeout = Duration(seconds: 120);
}
